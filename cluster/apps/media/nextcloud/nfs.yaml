---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-data
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  mountOptions:
    - nfsvers=4.1
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1T
